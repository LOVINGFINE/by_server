name: by_server
description: by_server application.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: ">=2.18.1"

dependencies:
  http_server: ^1.0.0
  mongo_dart: ^0.7.2
  encrypt: ^5.0.1
  shelf: ^1.2.0
  shelf_router: ^1.1.2
  shelf_cors_headers: ^0.1.2
  shelf_web_socket: ^1.0.2
  yaml: ^3.1.1 
  mailer: ^5.2.0
  shelf_static: ^1.1.1

dev_dependencies:
  lints: ^1.0.0
  build: ^2.3.1
  source_gen: ^1.2.3
  build_runner: ^2.0.0
